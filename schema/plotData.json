{
  "type": "object",
  "description": "Daily plot data and metadata for a specific region ID and variable ID (found in filename by convention).",
  "properties": {
    "metadata": {
      "properties": {
        "minYear": {
          "type": "integer",
          "description": "The water year in which the average (depending on the variable, could be median, mean, or sum) of all days in the water year represents a historical minimum for this region and variable."
        },
        "maxYear": {
          "type": "integer",
          "description": "The water year in which the average (depending on the variable, could be median, mean, or sum) of all days in the water year represents a historical maximum for this region and variable."
        }
      },
      "required": ["minYear", "maxYear"],
      "additionalProperties": false
    },
    "data": {
      "type": "object",
      "description": "A mapping of column headers to arrays of column values. It's expected that each array will be the same length.",
      "properties": {
        "dayOfWaterYear": {
          "type": "array",
          "items": { "type": "integer", "minimum": 1, "maximum": 366 },
          "description": "The day of water year for this row, starting with 1."
        },
        "date": {
          "type": "array",
          "items": { "type": "string", "format": "date" },
          "description": "The calendar date (YYYY-MM-DD) for this row."
        },
        "yearToDate": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Observed values for the current water year to date."
        },
        "min": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Observed values for the minimum water year (as specified in `metadata.minYear`)."
        },
        "max": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Observed values for the maximum water year (as specified in `metadata.maxYear`)."
        },
        "median": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Historical median for each day of the water year."
        },
        "prc25": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Historical 25th percentile for each day of the water year."
        },
        "prc75": {
          "type": "array",
          "items": { "type": ["number", "null"] },
          "description": "Historical 75th percentile for each day of the water year."
        }
      },
      "required": [
        "dayOfWaterYear",
        "date",
        "yearToDate",
        "min",
        "max",
        "median",
        "prc25",
        "prc75"
      ],
      "additionalProperties": false
    }
  },
  "required": ["data", "metadata"],
  "additionalProperties": false
}
